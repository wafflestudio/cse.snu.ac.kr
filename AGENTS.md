# 디자인 시스템 작업

디자인 시스템 작업을 시작하거나 이어갈 때는 [작업 계획](docs/design-system/PLAN.md)을 먼저 읽는다.
상단의 현재 단계와 다음 작업을 기준으로 진행하고, 작업 종료 시 실제 결과에 맞춰 갱신한다.

이전 초안은 `tmp/design-system`에 보존되어 있다. 참고 자료이며 새 작업의 확정된 디자인 규칙이 아니다.
새 작업은 사용자와 처음부터 조사하고 결정한다. 합의된 범위의 구현·검증은 이어서 완료한다.

사용자가 판단하거나 답해야 할 질문이 생길 때까지 계획 안의 다음 작업을 계속 진행한다.
중간 산출물을 만들었다는 이유만으로 턴을 끝내고 사용자의 ‘계속’ 지시를 기다리지 않는다.
질문이 필요하면 먼저 근거와 비교할 결과물을 준비한다. 진행 중인 작업과 아직 시작하지 않은 작업을 정확히 구분해 알린다.

새 질문만 연속해서 제시하지 않는다. 중간 검토 때마다 확정된 DS 내용과 실제 견본을 먼저 보여주고,
방향만 결정한 규칙·앱에 적용한 규칙·아직 미확정인 내용을 구분한다.
현재 DS의 누적 내용은 `docs/design-system/index.html`, 진행 현황은 `PLAN.md`에서 함께 확인할 수 있게 유지한다.

환경과 기술 규칙은 루트 `CLAUDE.md`, 프론트는 `apps/web/CLAUDE.md`, E2E는 `e2e/CLAUDE.md`를 확인한다.

디자인 시스템 작업 중 발견한 기능·주요 로직 버그는 우선 계획의 후속 목록에 기록하고,
별도 요청 없이 수정 범위를 넓히지 않는다. 당장은 디자인 변경을 우선한다.
SunEditor 내부 동작·CSP·에디터의 서체/서식 차이와 Sonner 수정은 현재 범위에서 제외한다.
