# production (main push). deploy.yml 은 SSH_ 접속 정보만 읽고, 나머지는 호스트의 host-deploy.sh 가 읽는다.
SSH_HOST=147.46.92.120
SSH_PORT=9122
SSH_USER=waffle
URL=cse.snu.ac.kr
PROFILE=prod
# 웹 vite 빌드 mode
WEB_MODE=production
CADDYFILE=caddy/Caddyfile

# Caddy 가 쓰는 인증서 경로
CERTIFICATE=/certs/Wildcard.snu.ac.kr_pem.pem
PRIVATE_KEY=/certs/KeyFile_Wildcard.snu.ac.kr_pem_unlocked.key
