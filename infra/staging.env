# staging (develop push). deploy.yml 이 읽고 host-deploy.sh 로 넘긴다.
SSH_HOST=168.107.16.249
SSH_PORT=22
SSH_USER=ubuntu
URL=168.107.16.249.nip.io
PROFILE=dev
# 웹 vite 빌드 mode
WEB_MODE=staging
CADDYFILE=caddy/Caddyfile.dev
# 인증서 경로 없음 — nip.io 도메인이라 Caddy 가 스스로 발급받는다.
