# 5단계 일반 시각 통합 검증

현재: DS-040 실제 적용·6승인견본일치/다른24장면동일 확인. 시각 기준66개 갱신. 전체 재실행132통과·1재시도통과·7실패 후 마지막2개 기준 갱신 및 해당2테스트 통과. 기존/혼합 차이5개는 남는다. 새 검토는 [BR-Q1 긴 경로 표시](breadcrumb.html), 앱 미적용. 아래 첫 실행 결과는 이력이다.

2026-09-24. [실제 견본](integration.html). 접근성 후속은 아직 재개하지 않았다.

## 실행 범위

- 현재 소스의 프로덕션 빌드, 로컬 실제 API, 고정 Playwright1.57 컨테이너.
- 공식 read/read-mobile 프로젝트·스펙·기준 이미지 사용. 기존 캐시 빌드의 서버 기동과 로컬 API 주소만 환경에 맞췄다. 공식 globalSetup으로 로컬 DB를 리셋·시드했다. 워커4·재시도2.
- 전체읽기140개:68통과·1재시도통과·71실패. 최종 실패70개는 이미지 비교,1개는 브라우저 SIGSEGV 기동 실패(앞선2시도에서는 이미지 차이). 기능 assert 실패 없음. 전체 통과로 처리하지 않는다.
- 이미지 차이의 파일별 분류를 [READ-REGRESSION.md](READ-REGRESSION.md)에 정리했다. 승인 DS 기반 갱신 후보64개, 공식 재실행 뒤 확인할 개인정보 모바일1개, 목록/상세 마스크·참가자 모바일 제목 관련 보류6개다. 목록만 워커1·재시도0으로 분리 실행하면4개 중3개 통과, 새소식 데스크톱1개 실패였다. 모바일2건은 이 실행에서 재현되지 않았다. 실행 전후 조회수가 달라졌으므로 동시성이 원인이라고 단정하지 않는다. 기존 기준 이미지는 아직 갱신하지 않았다.
- 추가 공개14장면과 관리2장면을 확보했다. 공개의영어320/390px·768px·1023/1024px, 한국어1280px와예약1023px, 관리1200px의오류/긴첨부/편집이탈을 포함한다.16장면 문서 가로폭에서 넘침 없음. 16개 PNG를 직접 검토한 결과 새 겹침·잘림이나 승인 DS와 다른 표현은 확인하지 못했다. 모든 언어·경로·데이터 조합을 검사한 것은 아니다.

## 관찰과 제한

- 1023/1024px 교과목 목록/카드와 예약3일/7일 전환은 기존 역할 구분을 유지한다.
- 영어 교과목 카드에서 HTML 태그가 설명 글자로 표시되는 현상은 기존 Q-14다. 기능·콘텐츠 표시 조사에 남기며 수정하지 않았다.
- 영어 페이지에서도 기존 한국어 관리 필드명·일부 필터명·시드 콘텐츠가 남는다. 이번 검증을 번역 전수 완료로 해석하지 않는다.
- 확인창 전체 페이지 캡처는 현재 뷰포트 밖에 오버레이가 없는 것처럼 보이므로 실제 확인용은 뷰포트로 저장했다.
- 임시 캡처에서 처음 사용한 공지 오류 문구 선택자/파일 입력 대상과 태그 토글을 바로잡고, 실제 제목 오류·첨부파일명·태그 선택 상태를 확인한 이미지를 저장했다. 앱 수정으로 해결한 문제가 아니다.
- SunEditor 내부·CSP·Sonner 내부·기능 버그는 작업 범위에서 제외했다. 초점·키보드 후속은 마지막에 별도로 검증한다.

- 짧은 한국어 인사말 시드에서는 오른쪽 메뉴가 푸터 바탕까지 내려오지만 텍스트와 충돌하지 않는다. 출발점에도 같은 배치 코드가 존재하므로 이번 DS 회귀로 분류하지 않는다.

## 작성·관리·언어 검증

같은 공식 설정의 flow/language/security 프로젝트를 로컬 DB 재시드 후 실행했다. 읽기 의존 프로젝트는 이미 실행했으므로 중복 실행하지 않았다. **79개 모두 통과**했다. 공지·뉴스·교과목·인물·연구실 등의 작성/편집/삭제와 단건/반복 예약 취소를 포함한다. 공식 보안 회귀를 실행한 것이며 CSP 등 보안 로직을 수정한 것은 아니다.

## 새 판단과 수정하지 않을 항목

- [LT-Q1 긴 페이지 제목](long-title.html): 참가자 화면의 기존 가로 넘침을 한영320/390px에서 확인했다. 문구·24px를 유지하고 괄호 앞에서 줄바꿈하는 비교를 준비했으며 앱에는 아직 미적용이다.
- 예약 개인정보 표는320/390px에서 실제 가로 휠로 마지막 열을 읽을 수 있어 기존 가로 스크롤을 유지한다. 정지 캡처의 부분 노출을 읽기 불가로 처리하지 않는다. 공유 본문/에디터 CSS를 추가 수정하지 않았다.
- 기준 이미지 갱신과 후속 회귀는 계속 남는다. 새 시각 결정을 먼저 검토하며 접근성 작업은 아직 재개하지 않는다.

예약 용어·확인창 적용과 첫 통합 결과는 로컬 커밋 `46103260`에 기록했다. 타입 검사·변경 코드 Biome·knip 통과. push/deploy는 하지 않았다.

## DS-040 적용 및 BR-Q1

DS-040 적용은 참가자 한영320/390/1280px에서 승인 견본과 일치했다. 다른4경로24개 장면은 기존 이미지와 동일했다. 일반 제목에 일괄 폭 제한을 넣는 중간 시도는 영어 교과목320px의 경로를 밀어내 최종 구현에서 제외했다. 최종 폭 제한은 괄호 줄바꿈 지점이 있는 제목에만 연결했다.

기존 긴 경로8개를 한영320/390px에서 조사한32상태 중13상태가 넘쳤다. [BR-Q1](breadcrumb.html)은 단계 단위 줄바꿈과 그래픽 공간을 비교하며 아직 앱에 적용하지 않았다. 개인정보 표는 기존 스크롤 유지다. 접근성 후속은 재개하지 않았다.
