# 주요 실행의 색 — C-Q7 제안

[현재 DS](index.html) · [실제 화면 비교](action-colors.html) · [진행 현황](PLAN.md)

**DS-004의 짙은 중립색 방향은 이미 확정했다. 이번 판단은 실제 값·호버와 첫 적용 범위다. 앱에는 아직 적용하지 않았다.**

## 현재 문제 → 변경 이유 → 기대 효과

같은 추가 행동인데 교수·직원·새 소식은 짙은 회색, 연구실·장학금·시설 등은 주황이다.
저장하기는 짙은 회색이고 예약하기는 주황이다. 현재 값만으로는 이 차이가 어떤 역할 차이인지 설명하기 어렵다.
DS-004에서는 일반적인 주요 실행에 짙은 중립색을 쓰기로 했지만 구체적인 값과 적용은 남겨뒀다.

기존 저장 버튼의 **#404040 바탕 + 흰 글자**, **호버 #737373 + 흰 글자**를 재사용하자.
새 회색을 늘리지 않고 이미 익숙한 저장 버튼과 추가·예약 버튼을 연결할 수 있다.
선택·그래픽의 주황은 유지하므로 작업을 시작하거나 완료하는 행동과 선택된 정보를 색 역할로 구분하기 쉬워진다.
이는 앞서 정한 역할 방향을 구체화하는 디자인 제안이다. 특정 회색이나 호버 농도가 접근성 필수값인 것은 아니다.
주황을 사용하는 면의 #ff6914와 흰 글씨 유지 결정(DS-007)은 그대로다.

## 제안 규칙

| 상태 | 제안 | 근거·한계 |
| --- | --- | --- |
| 기본 | 배경 neutral-700 #404040 / 글자 white | 기존 ui/Button neutral의 값 재사용 |
| 호버 | 배경 neutral-500 #737373 / 글자 white | 현재 저장 버튼처럼 마우스를 올리면 밝아짐. 새 상태색을 만들지 않음 |
| 비활성 | 위 색에 기존 opacity 40% 유지 | 새 비활성 규칙의 확정이 아님. 기존 disabled 조건·동작·크기 유지 |
| 키보드 초점 | 기존 버튼 표시 유지 | Text·TextArea의 DS-012를 버튼에 확대하지 않음. 공통 버튼 초점 규칙은 4단계 |
| 누름·처리 중 | 기존 구현 유지 | 별도 색 추가 없음. 컴포넌트별 검토는 4단계 |

이 색은 밝은 배경의 일반 주요 실행을 위한 기본값 제안이다.
위험 행동·어두운 배경·텍스트형 버튼은 역할과 배경을 따로 검토한다.
크기·문구·모서리·간격·클릭 동작은 이번 변경 대상이 아니다.

## 첫 적용 범위 — 추가·예약 11곳

승인되면 아래 `primary` 사용처만 기존 `neutral`로 바꾼다. 공용 `primary`의 색을 일괄 변경하지 않는다.

| 사용처 | 화면의 행동 | 검토 범위 |
| --- | --- | --- |
| research/labs/index.tsx | 연구실 추가 | 관리 데스크톱 |
| research/groups/index.tsx | 연구 스트림 추가 | 관리 데스크톱 |
| research/centers/index.tsx | 연구 센터 추가 | 관리 데스크톱 |
| academics/$studentType/scholarship/index.tsx | 장학금 추가 | 학부·대학원 관리 데스크톱 |
| academics/-components/courses/CoursesPage.tsx | 새 교과목 | 학부·대학원 공통 관리 데스크톱 |
| about/facilities/index.tsx | 시설 추가 | 관리 데스크톱 |
| about/student-clubs/index.tsx | 동아리 추가 | 관리 데스크톱 |
| about/future-careers/-components/CareerCompanies.tsx | 기업 추가 | 관리 데스크톱, 기존 비활성 조건 유지 |
| about/future-careers/-components/CareerStat.tsx | 연도 추가 | 관리 데스크톱 |
| reservations/-components/ReservationCalendar/CalendarToolbar.tsx | 예약하기 — 창 열기 | 일반 이용자 데스크톱·모바일 |
| reservations/-components/ReservationCalendar/AddReservationModal.tsx | 예약하기 — 제출 | 일반 이용자 데스크톱·모바일, 기존 비활성 조건 유지 |

앞 경로는 모두 `apps/web/src/routes/$locale/` 아래다.
`primary` 사용처 총 13곳 중 나머지 2곳은 공지 편집/완료 전환과 ui/ErrorState의 재시도·오류 행동이다.
추가·예약과 역할이 달라 이번 일괄 적용에서 제외하고 4단계로 연결한다.
기존 neutral의 삭제·확인·목록 이동까지 모두 “주요 실행”으로 승인하는 결정도 아니다.
버튼 API 이름과 역할 대응의 정리는 4단계에 남긴다.

## 비교와 확인 범위

`686fd92c`의 프로덕션 프론트와 기존 로컬 API, Playwright 1.57.0 Linux Chromium을 사용한다.
한국어 연구실 추가 1280px, 예약 열기 1280·390px, 예약창의 비활성 제출 1280px을 비교한다.
제안은 같은 DOM에서 배경 클래스를 기존 neutral과 같게 바꾼 캡처이며 앱 구현 결과가 아니다.
기본·호버 색, 버튼의 크기·글자색·불투명도 유지를 대조한다. 예약 제출은 하지 않는다.
[측정 기록](action-comparison/measurements.json).

나머지 사용처와 영어는 소스상 범위 조사이며 실제 렌더 확인 완료로 표시하지 않는다.
승인 후 변경된 프로덕션 빌드에서 적용 범위를 확인한다. 전체 회귀와 접근성 검증은 별도다.
기존 모바일 예약 배치 문제 Q-10은 이번 색 변경으로 해결하지 않는다.

## 색 검토의 남은 범위

오류 글자까지 DS-013으로 확정했고, 이번 C-Q7이 주요 실행의 값·첫 적용 범위를 잇는다.
입력 기본 경계, 선택·비활성·누름·초점의 컴포넌트별 조합은 아직 미확정이다.
각 컴포넌트의 형태·바탕과 함께 4단계에서 검토하고, 3-2를 마칠 때 이 경계를 명시한다.
색 검토 뒤에는 3-3 타이포 위계로 이동한다. 타이포 위계·아이콘·레이아웃은 아직 시작하지 않았다.

## 이번 판단

**추가·예약 11곳에 기본 #404040 / 호버 #737373 / 흰 글자를 적용할까?**
이미 선택한 방향을 다시 묻는 것이 아니라 실제 값과 적용 범위를 확인하는 질문이다.

## 검토 문서 확인

Playwright 1.57.0 Linux Chromium의 1280·390px에서 누적 DS·오류 기준·이번 비교 문서를 확인했다.
8개 비교 이미지 전환·로딩, 제안 견본의 기본·호버 색, 문서 내부 링크와 가로 넘침 없음을 확인했다.
이는 비교 문서 확인이며 앱 적용 검증을 대신하지 않는다.
