# 배치·간격 — 3-5 조사와 L-Q1

[누적 DS](index.html) · [현재 비교](layout.html) · [계획](PLAN.md)

## 지금의 상태

3-4 기반 아이콘의 기존 구성·승인한 첫 사용처 적용을 정리하고 3-5를 시작했다.
DS-021 연구실 자료 아이콘은 한·영 두 폭의 실제 이미지가 승인 견본과 일치했고 공통 초점도 연결됐다.
컴포넌트 전체 상태는 4단계에 남는다. 이번 배치 조사는 공개 공통 틀부터 시작하며 편집·관리 배치는 아직 새로 실측하지 않았다.

## 기존 기준과 책임 — 유지 원칙 DS-016에 따른 정리

| 층위 | 현재 값·역할 | 현재 처리 |
| --- | --- | --- |
| 공개 화면 전환 | CSS sm 640px, JS 모바일 판정 639.98px 이하 | 현재 구현의 관찰. 적합한 새 전환값으로 확정한 것이 아님 |
| 루트 최소 폭 | 640px부터 body 최소 1200px | 중간 폭에서 페이지 전체 가로 넘침 재현. 별도 수정안 필요 |
| 일반 본문 가로 | 모바일 좌우 20px / 데스크톱 본문 틀 안쪽 왼쪽 100px·오른쪽 360px | 기존 유지. 오른쪽 값은 보조 내비 공간을 포함. 바깥 좌측 내비 자리와 구분 |
| 일반 본문 세로 | 모바일 위 28·아래 64px / 데스크톱 위 44·아래 150px | PageLayout 소유. none/noTop/noBottom 변형이 담당하지 않는 쪽은 페이지가 소유 |
| 제목·보조 내비 | 제목 위 54px, 제목 아래 24/44px, 보조 내비 right 80px·sticky top 52px | 현재 맥락 유지. 숫자 차이만으로 통합하지 않음 |
| 푸터 | 상단 모바일 좌우 24·위아래 36px, 데스크톱 좌우 60·위아래 40px | 본문과 역할이 달라 20px로 일괄 통일하지 않음 |
| 폼 필드 사이 | Fieldset 10·16·20·24·32·40·44·48px, 라벨 아래 4·8·12px | 소스 관찰. 사용처별 역할 대조·데스크톱 실측은 남음 |
| 요소 내부 | 아이콘+문구·버튼 묶음·목록 행의 기존 gap | 각 컴포넌트가 소유. 신규 단일 간격 스케일 미결정 |
| 그래픽 접합 | .62/.625rem 등 작은 보정 | 기존 유지. 순수 반올림 오차인지 확인 없이 정규화하지 않음 |

출처: app.css, PageLayout, PageTitle, SubNavbar, Footer, form/Fieldset, useResponsive, __root, [기존 역할 조사](ROLE-AUDIT.md).
값 표는 현재 구현의 책임을 명확히 한 것이며 모든 폭에서 배치가 적합하다는 뜻은 아니다.
새 화면에서는 기존 동일 역할의 배치를 먼저 재사용한다. 예외는 겉보기 숫자보다 콘텐츠·탐색의 필요를 기준으로 설명한다.

## 실제 재확인

현재 프로덕션 빌드의 학부 소개를 한국어·영어 각각 320·360·390·639·640·768·1024·1199·1200·1280px에서 확인했다.
[20개 상태 기록](layout-audit/measurements.json). 320px는 조사 표본이며 제품의 최소 지원 폭을 새로 확정한 것이 아니다.

| 화면 | 기존 문서 폭 | 관찰 |
| --- | --- | --- |
| 한국어 320·360·390·639px | 뷰포트와 동일 | 이 공통 틀 표본에서 전체 가로 넘침 없음 |
| 영어 320·360·390px | 436px | 푸터 More의 긴 링크가 열 밖·화면 밖으로 나감 |
| 영어 639px | 728px | 한 줄에 더 많은 그룹이 배치돼 More가 더 오른쪽으로 넘침 |
| 두 언어 640·768·1024·1199px | 1200px | 루트 최소 폭 때문에 전체 가로 스크롤. 푸터 줄바꿈만으로 해결되지 않음 |
| 두 언어 1200·1280px | 뷰포트와 동일 | 이 표본의 현재 데스크톱 배치 유지 가능 |

중간 폭의 최소 폭 제거만 먼저 하면 본문·좌우 탐색·표·예약 구성이 충돌할 수 있다.
푸터와 구분해 헤더·본문·탐색·표/달력을 함께 비교하고 전환 범위를 결정해야 한다. 이 문제를 해결 완료로 처리하지 않는다.

## L-Q1 — 모바일 푸터의 긴 링크 줄바꿈

**문제:** Footer 그룹의 링크에 `whitespace-nowrap`가 붙어 있어 긴 영어 항목이 고정 열을 벗어난다.
**변경 이유:** 링크 끝을 읽으려면 페이지를 가로로 움직여야 한다. 서체·크기를 줄이거나 이름을 생략할 이유보다 배치 제약을 푸는 이유가 명확하다.
**추천:** 기존 모바일 범위(<640px)에서 링크가 자기 열 안에서 단어 단위로 줄바꿈하도록 허용한다. 기존 그룹 폭·순서·간격·서체·색과 데스크톱 한 줄 표시는 유지한다.
**효과:** 영어 320·360·390·639px 표본에서 문서 폭이 화면 폭과 같아졌다. 길어진 항목만 여러 줄이 되고 푸터 높이가 늘어난다.
**대가·한계:** 영어 More의 일부 항목은 2~3줄이 된다. 데스크톱과 중간 폭의 1200px 최소 폭 문제는 별도다. 임의의 긴 URL·단어를 강제로 글자마다 자르는 제안은 아니다.

[한·영 320·390·1280px 비교](layout.html)는 실제 앱 DOM에서 모바일 링크의 white-space만 임시 변경한 견본이다. 앱 소스는 아직 미변경이다.
한국어와 데스크톱 비교 이미지는 전후 동일했다. 링크 이름·목적지·그룹 순서는 바뀌지 않는다.

**이번 판단: 모바일 푸터의 긴 링크를 기존 열 안에서 자연스럽게 줄바꿈하도록 할까?**

## 다음 조사

1. L-Q1 승인 범위 적용·확인.
2. 640~1199px 공개 배치: 기존 모바일 구성 연장과 중간 폭 전용 조정을 실제 본문·탐색·표/예약에서 비교. JS 분기·이미지 sizes도 함께 대조.
3. 데스크톱 편집·관리의 필드 묶음과 간격 책임 확인. 모바일 편집 전용 최적화는 DS-003에 따라 제외.
4. 본문·선택형·인물·메인·카테고리의 간격 역할·예외 정리. 문제 없는 기존 값은 유지하고 3-6 모양·크기로 연결.

검토 문서는 1280·390px의 가로 넘침·내부 링크·한영/화면 폭 이미지 전환을 확인했다.
